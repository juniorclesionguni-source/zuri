# Copia para .env.local e preenche com os valores do dashboard Supabase.
# Sem estes valores, a app corre 100% em modo mock (sem backend).
# A anon key é pública por design — a RLS é que protege os dados.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
